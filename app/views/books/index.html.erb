<p id="notice"><%= notice %></p>

<h1>Libros</h1>

<table>
  <tr>
    <tr>
      <th>Titulo</th>
      <th>Autor</th>
      <th>ISBN</th>
      <th>ASIN</th>
      <th colspan="3"></th>
    </tr>
  </tr>

  <% @books.each do |book| %>
    <tr>
      <td><%= book.title %></td>
      <td><%= book.author %></td>
      <td><%= book.isbn %></td>
      <td><%= book.asin %></td>
      <td><%= link_to 'Mostrar', book_path(book) %></td>
      <td><%= link_to 'Editar', edit_book_path(book) %></td>
      <td>
        <%=
          link_to(
            'Borrar',
            book,
            method: :delete,
            data: { confirm: '¿Estás seguro?' }
          )
        %>
      </td>
    </tr>
  <% end %>
</table>

<br>

<%= link_to 'Agregar nuevo libro', new_book_path %>
